# URL du backend CFNG (Railway)
API_URL=https://api.cfng.fr
NEXT_PUBLIC_API_URL=https://api.cfng.fr

# Slug de l'artisan dans la plateforme
ARTISAN_SLUG=thalie-creations

# Secret partagé avec le backend pour sécuriser les appels /api/revalidate
# Doit correspondre à la valeur configurée côté backend (CFNG_REVALIDATE_SECRET)
REVALIDATE_SECRET=
